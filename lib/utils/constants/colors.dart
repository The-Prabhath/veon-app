import 'package:flutter/material.dart';

class TColors {
  TColors._();

  // ========================
  // App Basic Colors
  // ========================
  static const Color primary = Color(0xFF000000); // Pure Black
  static const Color secondary = Color(0xFFD4AF37); // Luxury Gold
  static const Color accent = Color(0xFFF5E6C8); // Soft Gold highlight

  // ========================
  // Text Colors
  // ========================
  static const Color textPrimary = Color(0xFFFFFFFF); // White (for dark UI)
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color textWhite = Colors.white;

  // ========================
  // Background Colors
  // ========================
  static const Color light = Color(0xFFF8F8F8); // optional light mode
  static const Color dark = Color(0xFF0B0B0B);
  static const Color primaryBackground = Color(0xFF0E0E0E);

  // ========================
  // Container Colors
  // ========================
  static const Color lightContainer = Color(0xFFFFFFFF);
  static const Color darkContainer = Color(0xFF1A1A1A);

  // ========================
  // Button Colors
  // ========================
  static const Color buttonPrimary = Color(0xFFD4AF37); // Gold CTA
  static const Color buttonSecondary = Color(0xFF1C1C1C);
  static const Color buttonDisabled = Color(0xFF444444);

  // ========================
  // Border Colors
  // ========================
  static const Color borderPrimary = Color(0xFF2A2A2A);
  static const Color borderSecondary = Color(0xFF1A1A1A);

  // ========================
  // Status Colors
  // ========================
  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFFA000);
  static const Color info = Color(0xFF1E88E5);

  // ========================
  // Neutral Shades
  // ========================
  static const Color black = Color(0xFF000000);
  static const Color darkerGrey = Color(0xFF1A1A1A);
  static const Color darkGrey = Color(0xFF2E2E2E);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color softGrey = Color(0xFFECECEC);
  static const Color lightGrey = Color(0xFFF5F5F5);
  static const Color white = Color(0xFFFFFFFF);

  static const Color red = Colors.red;
}
